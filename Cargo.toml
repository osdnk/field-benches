[package]
name = "bin-fields"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib"]

[dev-dependencies]
criterion = "0.5"
rand = "0.8"
rand_chacha = "0.3"

[[bench]]
name = "mul"
harness = false

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
debug = true

[profile.bench]
opt-level = 3
lto = "fat"
codegen-units = 1
debug = true
